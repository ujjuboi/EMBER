name = "ember-relay"
main = "index.ts"
compatibility_date = "2024-11-01"

[durable_objects]
bindings = [{ name = "RELAY", class_name = "RelayRoom" }]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RelayRoom"]

[vars]
DESCRIPTION = "EMBER partner sync signaling relay (SDP/ICE only)"